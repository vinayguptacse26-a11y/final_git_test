# Sample Project for AI Git Push Testing

This repository contains multiple files to test how the AI Git push tool handles a more complex directory structure, including `.env` variables and `.gitignore` rules.
